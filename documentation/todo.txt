//higher up higher priority

symbol table must
add function comments

add parsing support for negative numbers

add pointers
add functions
add arrays
add option to initialize variables in for loop
add single line if stamtements example:
    if condition //no '{' needed
        code
    //end of if even tho no '}'
